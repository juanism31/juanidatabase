<!DOCTYPE html>
<html>
  <!DOCTYPE html>
<html lang="en">
<%-include('./partials/head.ejs')%>
<body>
    <section  class="contenedorApp">

    <%-include('./partials/header.ejs')%>
</section>
    <section>
      <h2><%=genero.name  %> </h2>
      <% peliculas.forEach(pelicula => { %>
        <% if(pelicula.GenreId === genero.id){ %> 
          <a class=linkpeli href="/pelicula/<%=pelicula.id%>"><p><%=pelicula.title %> </p></a>
       
        <% } %>  
      <% }) %> 
  

    </section>
  </body>
</html>
